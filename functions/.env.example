# RevenueCat project used only by the account-deletion callable. Set this with
# the Firebase CLI or deployment environment; never place the secret API key
# in this file.
REVENUECAT_PROJECT_ID=proj_replace_me

# REVENUECAT_SECRET_API_KEY must be created as a Cloud Functions secret:
# firebase functions:secrets:set REVENUECAT_SECRET_API_KEY
