/**
 * Fail-closed source for tests and release bundles. Metro may replace this
 * module with the git-ignored `.local.ts` sibling only in a debug bundle.
 */
export const revenueCatTestStoreApiKey = '';
