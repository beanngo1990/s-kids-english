export const REMOTE_APP_UPDATE_POLICY_KEY = 'app_update_policy_v1';

export const DEFAULT_REMOTE_APP_UPDATE_POLICY = JSON.stringify({
  schemaVersion: 1,
  enabled: false,
});

export const OPTIONAL_UPDATE_REMINDER_DELAY_MS = 3 * 24 * 60 * 60 * 1000;

